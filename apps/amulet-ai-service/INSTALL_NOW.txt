═══════════════════════════════════════════════════════
   ติดตั้ง Dependencies เดี๋ยวนี้!
═══════════════════════════════════════════════════════

รันคำสั่งนี้ทีละบรรทัด:

cd C:\wat\pra-analysis\pra-analysis\apps\amulet-ai-service
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install fastapi==0.115.0 uvicorn==0.30.6 pydantic==2.9.2
pip install "sqlalchemy>=2.0.30"
pip install pytest==7.4.3 pytest-asyncio==0.21.1 pytest-cov==4.1.0 httpx==0.25.2
pip install psycopg2-binary==2.9.9 pillow==10.4.0 python-multipart==0.0.12
pip install structlog==24.1.0 qdrant-client==1.9.2 minio==7.2.0

หรือรัน script:
.\INSTALL_MINIMAL.ps1

จากนั้นรัน tests:
pytest tests/ -v --no-cov

═══════════════════════════════════════════════════════

